/**
 * The ARK maps now live beside the catalog, which describes them whether or not
 * Game servers is installed. Kept so the game's own modules read the same path.
 */

export * from "@/lib/apps/ark-maps";
