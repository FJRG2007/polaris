/**
 * The copy button lives in @polaris/ui now, where the DNS record card needs it
 * too. Kept here so the screens that import it from this path still do.
 */

export { CopyButton } from "@polaris/ui";
